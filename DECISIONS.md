# Decisions

## Registry

| ID | Title | Disposition | Date | Context | Decision | Rationale | Status | Superseded By |
|-|-|-|-|-|-|-|-|-|
| D-BACKEND-001 | Backend seam is raw SSE bytes; decoding is shared and downstream | accepted | 2026-08-27 | feature/loop-engine | Backend.generate yields raw SSE byte chunks; a dedicated sse.py decoder parses them in the client, not the backend. ReplayBackend replays captured bytes verbatim. | A decoded-event seam would force the stub to re-serialize model JSON and diverge from the wire. A raw-bytes seam lets the stub replay real captured transport, keeping stub and live paths byte-identical. Rejected a decoded-event seam. | active |  |
| D-CONTRACT-001 | Builder-adapter contract is an external stub with a status mapping | accepted | 2026-08-27 | feature/loop-engine | contract.py ships the signature build(task_spec, worktree, context_tier) returning {status, files_changed, notes, telemetry} as a stub, plus to_build_status mapping loop Status to BuildStatus (DONE to DONE, EXHAUSTED to NEEDS_CONTEXT, DERAILED and BLOCKED to BLOCKED). | The contract is owned cross-repo by claude-protocol; claude-local implements the loop behind it, not the contract itself. DONE_WITH_CONCERNS is unreachable locally. Rejected defining the contract here. | active |  |
| D-DERAIL-001 | Derail on consecutive exact large-n repetition, not global diversity | accepted | 2026-08-27 | feature/loop-engine | DerailGuard flags REPETITION when a fixed-size deque window of recent large-n token slices repeats exactly after a warmup, plus TOKEN_CAP and TIMEOUT reasons with an injected clock. | Global-diversity heuristics false-positive on legitimately repetitive code such as imports and boilerplate. Consecutive-exact-repeat targets the actual stuck-decode failure. Rejected entropy/global-uniqueness scoring. | active |  |
| D-KEEP-001 | Writable allowlist enforced by realpath containment (keep_only) | accepted | 2026-08-27 | feature/loop-engine | resolve_within rejects absolute candidates, resolves the real path, requires containment under the worktree root, and refuses symlinks; only the permitted impl path is writable, re-applied on snapshot restore. | A reply naming ../ or a symlink could escape the worktree or overwrite the oracle test. Realpath containment makes escape unrepresentable. Rejected string-prefix path checks, which .. and links defeat. | active |  |
| D-LOOP-001 | Whole-file edits with an immutable orchestrator-owned oracle test | accepted | 2026-08-27 | feature/loop-engine | The model returns a complete impl file as raw text; the loop applies it to the one permitted impl path only. The oracle test is frontier-authored, pinned in the stable prefix, and never in the model's writable set. | Diff/patch from a local model is brittle to apply and lets the model silently edit its own test to pass. Whole-file plus a keep_only writable allowlist makes test tampering unrepresentable. Rejected patch-based edits and model-authored tests. | active |  |
| D-LOOP-002 | Non-thinking generation by default under hard token/attempt/time caps | accepted | 2026-08-27 | feature/loop-engine | Generation runs non-thinking by default with a hard thinking cap; every attempt is bounded by max_tokens, max_attempts, and a wall-clock timeout carried in the Budget. | Local models burn unbounded decode on chain-of-thought with no payoff for a whole-file edit task. Bounded decode by construction is the derail guard's contract. Rejected open-ended thinking. | active |  |
| D-ORACLE-001 | Oracle verdict from pytest JUnit-XML with a pinned collected count | accepted | 2026-08-27 | feature/loop-engine | TestRunner parses pytest JUnit-XML (xml.etree) into (passed, failed, errors, collected, skipped). A score is valid iff collected equals TaskSpec.expected_tests; green iff valid AND failed==0 AND errors==0. | Stdout scraping is fragile and lets a model win by importing away tests, dropping collected silently. Pinning expected_tests catches deletion/skip as invalid, not green. Rejected exit-code and stdout scraping. | active |  |
| D-PERF-001 | The hard token cap, not streaming abort, is the real decode bound | accepted | 2026-08-27 | feature/loop-engine | Decode is bounded by the Budget hard token cap; streaming-abort on derail saves client wall-clock and drops the connection but does not itself bound server decode. KV-cache reuse benefits prefill, not the socket. | Context7-verified httpx: breaking a stream closes the connection, but the server may already have decoded. Treating abort as the bound would overstate savings. Rejected relying on abort for the decode budget. | active |  |
| D-PROMPT-001 | Stable KV-cacheable prefix is rules card plus spec plus immutable test | accepted | 2026-08-27 | feature/loop-engine | PromptBuilder assembles a byte-identical prefix (static rules card + spec + the immutable test) once per task; only distilled feedback varies in the tail. Feedback is byte-capped and absolute paths stripped. | Any per-call mutation of the prefix discards the server's prefill KV cache. Pinning the test in the prefix also blocks tampering. Rejected dynamic prefix content and un-capped feedback. | active |  |
| D-SNAPSHOT-001 | Keep the best-passing snapshot; restore the full src subtree | accepted | 2026-08-27 | feature/loop-engine | SnapshotStore records each attempt's src subtree ranked by (passed desc, errors asc, failed asc, index asc); the loop restores the best snapshot on exit. Restore syncs exactly, constrained by keep_only. | REPAIR feedback can regress a passing attempt, so without a best-so-far the loop can end worse than mid-run. Whole-subtree capture avoids partial-file drift. Rejected clobber-latest retry. | active |  |
| D-SSE-001 | Hand-roll SSE line-buffering, not httpx-sse | accepted | 2026-08-27 | SSE decoder (src/claude_local/sse.py) | Decode the SSE wire in-module with a buffered line reader; add no SSE library. | httpx-sse is a second dependency against the single-httpx ethos and still leaves the frame->event mapping and [DONE]/usage-trailer handling to hand-write anyway. The buffered reader emits events one line at a time, so the derail guard can abort mid-stream — behavior a library's internal buffering would hide. | active |  |
| D-TELEMETRY-001 | claude-local writes only the local half; char proxy on abort | accepted | 2026-08-27 | feature/loop-engine | Telemetry records the local half of the economy record (model, calls, completion tokens, model-seconds, tokens/sec, status, attempts). On streaming abort with no usage block, completion tokens fall back to a char-count proxy flagged tokens_estimated. | The orchestrator half and shared keys are owned by claude-protocol. Aborted streams carry no server usage, so a proxy keeps the record complete but honest. Rejected dropping aborted-call tokens, which undercounts cost. | active |  |

## Invalidations

| Decision | Target | Note | Status | Superseded By |
|-|-|-|-|-|

## Amendments

| Amending | Amended | Note | Status | Superseded By |
|-|-|-|-|-|
