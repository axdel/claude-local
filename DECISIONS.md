# Decisions

## Registry

| ID | Title | Disposition | Date | Context | Decision | Rationale | Status | Superseded By |
|-|-|-|-|-|-|-|-|-|
| D-BACKEND-001 | Backend seam is raw SSE bytes; decoding is shared and downstream | accepted | 2026-08-27 | feature/loop-engine | Backend.generate yields raw SSE byte chunks; a dedicated sse.py decoder parses them in the client, not the backend. ReplayBackend replays captured bytes verbatim. | A decoded-event seam would force the stub to re-serialize model JSON and diverge from the wire. A raw-bytes seam lets the stub replay real captured transport, keeping stub and live paths byte-identical. Rejected a decoded-event seam. | active |  |
| D-BACKEND-002 | HttpxBackend core stream invariants are non-overridable; sampling via passthrough | accepted | 2026-08-27 | Transport backend (src/claude_local/backend.py) | HttpxBackend always sends stream=true, stream_options.include_usage=true, and the budget's max_tokens; generation_params carries only server-specific sampling knobs and merges under those core keys, never over them. | Non-thinking/repetition params are model-specific (e.g. Qwen enable_thinking) — hardcoding them would invent wire params from memory and break other servers, so they pass through generation_params. Streaming, usage accounting, and the hard token cap (D-PERF-001) are loop invariants, so a core-keys-win merge makes them unbreakable by config. | active |  |
| D-BACKEND-003 | Unreachable or error-status server -> BackendUnavailable, a propagating harness fault | accepted | 2026-08-27 | feature/loop-engine | HttpxBackend.generate translates an unreachable server (httpx.RequestError) or a non-2xx status (httpx.HTTPStatusError) into BackendUnavailable at the transport boundary, chaining the originating httpx error as __cause__ and carrying the request URL and model. It propagates uncaught through client -> loop -> implement(), never mapped to a terminal Status — the same fail-loud contract as SandboxUnavailable (D-SANDBOX-001) and OracleError. | A running OpenAI-compatible server at base_url is a documented prerequisite (claude-local never serves one), so a down or erroring server is an unmet precondition, not a task outcome; folding it into a Status would let a missing prerequisite masquerade as a failed implementation. Distinct from FAULTED (D-FAULT-001), a *reachable* server's mid-stream SSE error frame. Rejected: a raw httpx leak (violates Arch Rule 3 — infra errors translate to domain errors at the boundary); a single 'except httpx.HTTPError' clause (erases the unreachable-vs-error-status reason distinction the two disjoint clauses preserve). | active |  |
| D-BOUNDARY-001 | Enforce BOUNDARY_MAP one-way flow via an import-linter exhaustive layers contract | accepted | 2026-08-27 | pyproject.toml [tool.importlinter]; BOUNDARY_MAP.md | Enforce the one-way module dependency flow BOUNDARY_MAP.md documents with an import-linter layers contract (6 layers, independent pipe-separated siblings) and exhaustive=true under containers=[claude_local], so every submodule must be placed. primitive check boundary-map delegates to this contract. | A layers contract enforces the acyclic one-way DAG structurally (no upward import, no cycle) instead of enumerating each allowed edge as a forbidden/whitelist contract would, which then drifts from the map. exhaustive makes an unplaced new module a hard failure, keeping map and package in lockstep — the exact drift this fix repaired. Rejected leaving BOUNDARY_MAP.md as unenforced prose: the pre-fix state, where the boundary check was a no-op. | active |  |
| D-CLIENT-001 | Aborted-stream cleanup relies on CPython refcount finalization, not an explicit close | accepted | 2026-08-27 | feature/loop-engine; ModelClient.generate in src/claude_local/client.py | generate() consumes the decode/byte-stream iterator inline and keeps no lingering reference, so an early-abort break drops its last reference and CPython refcount finalization throws GeneratorExit into the HttpxBackend stream context manager, closing the transport synchronously at the break. No explicit try/finally close is added. | Probe-verified: a real derail abort against a backend whose byte-stream generator records GeneratorExit fired its finally synchronously before generate() returned (no leak on CPython, the sole target runtime, MLX/uv). An explicit close would be defensive code for a runtime-handled path and cannot be F2P-proven (the unfixed code already closes), so it was rejected. The recorded constraint: cleanup depends on CPython refcounting; a non-refcounting runtime such as PyPy defers close to the next GC sweep, so a port must add an explicit stream close before reusing the warm client across generations. | active |  |
| D-CONTRACT-001 | Builder-adapter contract is an external stub with a status mapping | accepted | 2026-08-27 | feature/loop-engine | contract.py ships the signature build(task_spec, worktree, context_tier) returning {status, files_changed, notes, telemetry} as a stub, plus to_build_status mapping loop Status to BuildStatus (DONE to DONE, EXHAUSTED to NEEDS_CONTEXT, DERAILED and BLOCKED to BLOCKED). | The contract was to be owned by an external orchestrator; claude-local would implement the loop behind it, not the contract itself. DONE_WITH_CONCERNS is unreachable locally. Rejected defining the contract here. | inactive | D-ENTRYPOINT-001 |
| D-DERAIL-001 | Derail on consecutive exact large-n repetition, not global diversity | accepted | 2026-08-27 | feature/loop-engine | DerailGuard flags REPETITION when a fixed-size deque window of recent large-n token slices repeats exactly after a warmup, plus TOKEN_CAP and TIMEOUT reasons with an injected clock. | Global-diversity heuristics false-positive on legitimately repetitive code such as imports and boilerplate. Consecutive-exact-repeat targets the actual stuck-decode failure. Rejected entropy/global-uniqueness scoring. | active |  |
| D-DERAIL-002 | DerailGuard is chunking-invariant: line slices, char-count cap, position-armed warmup | accepted | 2026-08-27 | feature/loop-engine (src/claude_local/derail.py) | The repetition slice unit is the completed line (refining D-DERAIL-001's generic large-n slice). The token-cap proxy counts decoded chars, and the warmup arms each line by its absolute stream position — both computed from the concatenated text, with lines reconstructed across delta boundaries. | The derail verdict must not depend on how the server chunks the stream (CLAUDE.md inference-hot-path invariant). Delta count and a running post-delta char total both vary with chunking and would give different verdicts for identically-concatenated text; char count and absolute line position do not. Rejected delta-count token proxy and running-total warmup. | active |  |
| D-ENTRYPOINT-001 | Public entry point implement() -> Outcome is the owned front door | accepted | 2026-08-27 | feature/loop-engine | claude-local's public API is implement(spec, *, base_url, model, ...) -> Outcome, the owned composition root: it wires the whole loop, runs one bounded red->green cycle in a scratch worktree against an already-running OpenAI-compatible server, and returns Outcome(status, code, impl_path, files_changed, LocalEconomyRecord). No external contract adapter and no CLI. | The project became a standalone, public, Claude-Code-driven tool rather than a plugin behind an external orchestrator's contract. A bare-prompt CLI cannot run the verified red->green loop because the model may never author its own oracle test, so it was dropped. Supersedes the builder-adapter contract stub (D-CONTRACT-001); the orchestrator computes net-savings from the returned record. | active |  |
| D-FAULT-001 | FAULTED terminal status surfaces an upstream server fault | accepted | 2026-08-27 | feature/loop-engine | An SSE error frame terminates the run as a new terminal Status.FAULTED, carrying the wire message as `fault` unrenamed across GenerationResult, LoopResult, and Outcome. Classifier precedence: DONE > FAULTED > DERAILED > BLOCKED > EXHAUSTED. | A server-side fault differs from a model DERAILED/BLOCKED or budget EXHAUSTED; folding it into a proxy-token truncation or BLOCKED hides an actionable operator signal. One `fault` name flows through, mirroring `derail_reason` and rejecting a client-vs-outcome name split (canonical Rule 2). | active |  |
| D-KEEP-001 | Writable allowlist enforced by realpath containment (keep_only) | accepted | 2026-08-27 | feature/loop-engine | resolve_within rejects absolute candidates, resolves the real path, requires containment under the worktree root, and refuses symlinks; only the permitted impl path is writable, re-applied on snapshot restore. | A reply naming ../ or a symlink could escape the worktree or overwrite the oracle test. Realpath containment makes escape unrepresentable. Rejected string-prefix path checks, which .. and links defeat. | active |  |
| D-LOOP-001 | Whole-file edits with an immutable orchestrator-owned oracle test | accepted | 2026-08-27 | feature/loop-engine | The model returns a complete impl file as raw text; the loop applies it to the one permitted impl path only. The oracle test is frontier-authored, pinned in the stable prefix, and never in the model's writable set. | Diff/patch from a local model is brittle to apply and lets the model silently edit its own test to pass. Whole-file plus a keep_only writable allowlist makes test tampering unrepresentable. Rejected patch-based edits and model-authored tests. | active |  |
| D-LOOP-002 | Non-thinking generation by default under hard token/attempt/time caps | accepted | 2026-08-27 | feature/loop-engine | Generation runs non-thinking by default with a hard thinking cap; every attempt is bounded by max_tokens, max_attempts, and a wall-clock timeout carried in the Budget. | Local models burn unbounded decode on chain-of-thought with no payoff for a whole-file edit task. Bounded decode by construction is the derail guard's contract. Rejected open-ended thinking. | active |  |
| D-ORACLE-001 | Oracle verdict from pytest JUnit-XML with a pinned collected count | accepted | 2026-08-27 | feature/loop-engine | TestRunner parses pytest JUnit-XML (xml.etree) into (passed, failed, errors, collected, skipped). A score is valid iff collected equals TaskSpec.expected_tests; green iff valid AND failed==0 AND errors==0. | Stdout scraping is fragile and lets a model win by importing away tests, dropping collected silently. Pinning expected_tests catches deletion/skip as invalid, not green. Rejected exit-code and stdout scraping. | active |  |
| D-ORACLE-002 | Parse the oracle JUnit report with stdlib xml.etree, not defusedxml | accepted | 2026-08-27 | feature/loop-engine | score_junit parses the pytest JUnit report with stdlib xml.etree.fromstring; the subprocess spawn uses a fixed uv/pytest argv with no shell. bandit B314 / ruff S314 (xml) and S603 (subprocess) are suppressed inline with a scoped, documented rationale — never a blanket ignore. | The report is pytest's own junitxml output written to a temp dir the runner owns; pytest emits no DOCTYPE or entity definitions, so there is no XXE/entity-expansion surface, and the spawn argv is a constant with no user input. defusedxml was rejected: it would add a runtime dependency (the project's runtime dep is httpx only) to guard a structurally-closed surface — disproportionate (Rule 14). Suppression is scoped to the specific rule IDs, not a whole-file or blanket ignore. | active |  |
| D-ORACLE-003 | Oracle runs force PYTHONDONTWRITEBYTECODE so scoring never reads stale bytecode | accepted | 2026-08-27 | feature/loop-engine; found by the end-to-end loop ceremony | The oracle runner's default spawn sets PYTHONDONTWRITEBYTECODE=1 in the pytest subprocess environment, so no __pycache__ is written and every scoring run compiles the current impl source. | The loop rewrites the one impl file between attempts, frequently same-size (a one-operator REPAIR edit) and within one clock second; CPython validates a cached .pyc by (source size, second-granularity mtime), so a stale .pyc from the prior attempt is imported and scores the WRONG source — a silent oracle corruption. Rejected clearing __pycache__ before each run (misses nested caches, races the write) and PYTHONPYCACHEPREFIX (redirects the cache but it can still go stale the same way); never writing bytecode is the simplest guarantee of fresh compilation, on a cold path where the cost is irrelevant. | active |  |
| D-PERF-001 | The hard token cap, not streaming abort, is the real decode bound | accepted | 2026-08-27 | feature/loop-engine | Decode is bounded by the Budget hard token cap; streaming-abort on derail saves client wall-clock and drops the connection but does not itself bound server decode. KV-cache reuse benefits prefill, not the socket. | Context7-verified httpx: breaking a stream closes the connection, but the server may already have decoded. Treating abort as the bound would overstate savings. Rejected relying on abort for the decode budget. | active |  |
| D-PROMPT-001 | Stable KV-cacheable prefix is rules card plus spec plus immutable test | accepted | 2026-08-27 | feature/loop-engine | PromptBuilder assembles a byte-identical prefix (static rules card + spec + the immutable test) once per task; only distilled feedback varies in the tail. Feedback is byte-capped and absolute paths stripped. | Any per-call mutation of the prefix discards the server's prefill KV cache. Pinning the test in the prefix also blocks tampering. Rejected dynamic prefix content and un-capped feedback. | active |  |
| D-SANDBOX-001 | Run the oracle pytest subprocess under macOS sandbox-exec kernel confinement | accepted | 2026-08-27 | feature/loop-engine | The oracle runner's default spawn (sandbox.sandboxed_spawn) runs the pytest subprocess under macOS sandbox-exec with a deny-by-default SBPL profile: it may write only inside the disposable report box, reach no network, and inherits none of the parent environment beyond a minimal allowlist that carries no API tokens. Layered on top: a setrlimit CPU and single-file-size cap applied post-fork, and a wall-clock timeout that SIGKILLs the whole process group (the child leads its own session). Fail-closed: if sandbox-exec is absent the spawn raises SandboxUnavailable rather than running untrusted code unconfined. | The loop writes untrusted model text to the impl file and pytest imports (executes) it, so top-level code in a degenerate or hostile impl could otherwise delete files, exfiltrate secrets, or hang the loop. Confinement leaves the executed code able only to compute and write its one report - the design invariant that the implementer only uses itself, while destructive and network powers stay with the orchestrator. Rejected: running pytest unconfined (the removed prior default); an in-process, cap-only guard (cannot stop a fork/exec or a raw syscall); and adding a container or microVM runtime dependency now (disproportionate - sandbox-exec is a zero-dependency kernel front-end already present on every macOS host, and the microVM swap stays available behind the spawn seam for a future hostile-model threat tier). | active |  |
| D-SANDBOX-002 | Oracle sandbox: inline -p profile + poll-guarded group-kill teardown | accepted | 2026-08-27 | src/claude_local/sandbox.py | Pass the SBPL profile to sandbox-exec inline via -p (never an -f temp file); tear the process group down in a finally that SIGKILLs whenever the child is still live at exit — a timeout or any other interrupted communicate — and skips the kill once communicate has reaped it. | sandbox-exec reads an -f profile lazily post-fork, so the old finally-unlink raced that read and orphaned or incidentally killed the child (probed: unlink at 0.0s kills, at 0.5s survives). Inline -p removes the file, the race, and any policy file the confined child could rewrite. The poll guard fixes the non-timeout orphan leak and prevents a killpg against a recycled PID on the reaped success path. Confinement re-verified identical under -p. | active |  |
| D-SANDBOX-003 | Oracle sandbox rlimits: CPU/FSIZE only; RLIMIT_AS and NPROC excluded as non-viable on macOS | accepted | 2026-08-27 | src/claude_local/sandbox.py | Cap the oracle child with RLIMIT_CPU and RLIMIT_FSIZE only. Do not add RLIMIT_AS (address space) or RLIMIT_NPROC (process count) as sandbox caps. | On macOS setrlimit(RLIMIT_AS, ...) is rejected outright ('current limit exceeds maximum limit'), so it cannot be set. RLIMIT_NPROC is per-UID, not per-process — a fixed child cap cannot distinguish a forking child from the developer's live process count, which would make the oracle flaky. CPU and FSIZE default to unlimited, so their caps are meaningful; the kernel SBPL profile plus the wall-clock/group-kill teardown carry the rest of the containment. | active |  |
| D-SNAPSHOT-001 | Keep the best-passing snapshot; restore the full src subtree | accepted | 2026-08-27 | feature/loop-engine | SnapshotStore records each attempt's src subtree ranked by (passed desc, errors asc, failed asc, index asc); the loop restores the best snapshot on exit. Restore syncs exactly, constrained by keep_only. | REPAIR feedback can regress a passing attempt, so without a best-so-far the loop can end worse than mid-run. Whole-subtree capture avoids partial-file drift. Rejected clobber-latest retry. | active |  |
| D-SSE-001 | Hand-roll SSE line-buffering, not httpx-sse | accepted | 2026-08-27 | SSE decoder (src/claude_local/sse.py) | Decode the SSE wire in-module with a buffered line reader; add no SSE library. | httpx-sse is a second dependency against the single-httpx ethos and still leaves the frame->event mapping and [DONE]/usage-trailer handling to hand-write anyway. The buffered reader emits events one line at a time, so the derail guard can abort mid-stream — behavior a library's internal buffering would hide. | active |  |
| D-TELEMETRY-001 | claude-local writes only the local half; char proxy on abort | accepted | 2026-08-27 | feature/loop-engine | Telemetry records the local half of the economy record (model, calls, completion tokens, model-seconds, tokens/sec, status, attempts). On streaming abort with no usage block, completion tokens fall back to a char-count proxy flagged tokens_estimated. | The orchestrator half and shared keys are owned by the driving orchestrator. Aborted streams carry no server usage, so a proxy keeps the record complete but honest. Rejected dropping aborted-call tokens, which undercounts cost. | active |  |

## Invalidations

| Decision | Target | Note | Status | Superseded By |
|-|-|-|-|-|

## Amendments

| Amending | Amended | Note | Status | Superseded By |
|-|-|-|-|-|
