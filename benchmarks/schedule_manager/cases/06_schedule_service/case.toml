# Case 6 — role-aware schedule service: CRUD, ownership authorization, cron behavior.
impl_path = "app/services/schedule_service.py"
context_paths = [
    "app/cron.py",
    "app/repositories/schedule_repository.py",
    "app/repositories/user_repository.py",
    "app/schemas.py",
]

[budget]
max_attempts = 4
max_tokens = 10240
timeout_s = 180.0
