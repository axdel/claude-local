# Rung 5 — RBAC: identity resolution and administrator authorization.
impl_path = "app/security.py"
context_paths = [
    "app/services/auth_service.py",
    "app/dependencies.py",
]

[budget]
max_attempts = 4
max_tokens = 6144
timeout_s = 120.0
