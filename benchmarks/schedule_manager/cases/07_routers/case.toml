# Case 7 — schedule CRUD router: the full HTTP endpoint surface, pure delegation.
impl_path = "app/routers/schedules.py"
context_paths = [
    "app/dependencies.py",
    "app/repositories/schedule_repository.py",
    "app/schemas.py",
]

[budget]
max_attempts = 4
max_tokens = 8192
timeout_s = 150.0
