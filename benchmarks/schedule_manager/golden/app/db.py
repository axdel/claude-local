"""Database defaults consumed by the schedule-manager application factory."""

DEFAULT_DATABASE_URL = "sqlite:///:memory:"
"""Default to an isolated in-memory SQLite database for each app instance."""
